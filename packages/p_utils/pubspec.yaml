name: p_utils
description: Utilities package for DevNote app
version: 0.0.1

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  bloc: ^9.0.0
  dio: ^5.9.0
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  p_models: ^0.0.1
  talker: ^5.0.0

dev_dependencies:
  flutter_lints: ^6.0.0

  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4

resolution: workspace
